## Go程序的结构

### 一个Go程序包含哪些基本构件

和其他大部分编程语言一样，一个Go程序由函数、变量、常量、类型、关键字、表达式、文件和包组成。

Go的关键字有25个：

```
break    default    func    interface    select    case    defer    go    map    struct
chan     else       goto    package      switch    const   fallthrough    if     range
type     continue   for     import       return    var
```



此外，Go还有30多个预定义的名字，包括内建常量、类型和内建函数：

```
内建常量:    true    false    iota    nil
内建类型:    int     int8     int32   int64
            uint    uint8    uint32  uint64
            uintptr
            float32          float64
            complex128       complex64
            bool    byte     rune    string     error
内建函数:    make    len    cap    new    append    copy    close    delete
            complex rel    imag   panic  recover
```

注意：内建常量/函数和类型并不是关键字，你可以在自己的程序中可以重新定义他们，但是当你想要重新定义他们时，一定要确认知道自己在做什么。比如